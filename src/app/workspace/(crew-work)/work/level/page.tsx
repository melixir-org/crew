import { PageStoreProvider } from '@/provider/PageStore';

const Level = () => {
    return (
        <PageStoreProvider>
            <div>Level</div>
        </PageStoreProvider>
    );
};

export default Level;
