import { PageStoreProvider } from '@/provider/PageStore';

const Work = () => {
    return (
        <PageStoreProvider>
            <div>Work</div>
        </PageStoreProvider>
    );
};

export default Work;
