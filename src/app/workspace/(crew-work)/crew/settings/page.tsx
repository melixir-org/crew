import { PageStoreProvider } from '@/provider/PageStore';

const Settings = () => {
    return (
        <PageStoreProvider>
            <div>Settings</div>
        </PageStoreProvider>
    );
};

export default Settings;
