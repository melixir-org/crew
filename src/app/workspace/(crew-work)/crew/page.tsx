import { PageStoreProvider } from '@/provider/PageStore';

const Crew = () => {
    return (
        <PageStoreProvider>
            <div>Crew</div>
        </PageStoreProvider>
    );
};

export default Crew;
